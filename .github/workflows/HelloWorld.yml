name: Hello World
on: [push]
env:
  variable: var1
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      nombre : 15
    steps:
      - uses: actions/checkout@v3
      - name: Hello World
        run: echo Hello World !  $variable $nombre
        
